@model IEnumerable<PropertySurveyService.Models.Surveyor>

@{
    ViewData["Title"] = "Index";
}

<h1>Surveyors</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SurveyorCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SurveyorCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SurveyorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SurveyorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SurveyorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
